# Raw prediction data

The large class-I and class-II prediction CSV files are stored in the companion archive identified by `deposit_manifest.json`; they are not committed to Git. Job manifests and per-chunk retrieval metadata remain in this directory.

External prediction services can change. The archived CSVs, their checksums, and their retrieval metadata define the data state used by this analysis.
